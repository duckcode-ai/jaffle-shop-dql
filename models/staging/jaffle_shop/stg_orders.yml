kind: model
name: stg_orders
materialization: table
database: jaffle_shop
schema: main
description: Order data with basic cleaning and transformation applied, one row per
  order.
depends_on:
- source:
    source: jaffle_shop
    name: orders
columns:
- name: order_id
  description: The unique key for each order.
meta:
  datalex:
    dbt:
      unique_id: model.jaffle_shop.stg_orders
